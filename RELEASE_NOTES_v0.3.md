# v0.3

- 删除误导性的空“促销单定位”框。
- 新增三态来源模型：`verified`、`direct`、`unlocated`。
- 只有真实核验页码才显示“促销单准确定位”。
- 新增 `data/source_locators.json` 可审计覆盖层。
- 新增 `npm run locate` 和 `npm run build:preview`。
- 每日工作流在更新后应用定位覆盖并校验。
- 增加 Codex GitHub Pages 部署与 QA 任务。
