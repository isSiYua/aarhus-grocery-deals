{
  "name": "aarhus-grocery-deals",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Mobile-first Aarhus grocery deal pocketbook with daily incremental updates.",
  "scripts": {
    "update": "node scripts/update-data.mjs",
    "validate": "node scripts/validate-data.mjs",
    "test": "node --test",
    "serve": "python3 -m http.server 8080",
    "locate": "node scripts/apply-source-locators.mjs",
    "build:preview": "node scripts/build-preview.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
